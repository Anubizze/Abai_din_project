# 🔒 Безопасность

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**НИКОГДА не коммитьте секретные данные в Git!**

## 🚨 Что делать, если токен/ключи уже попали в Git?

Если вы уже закоммитили токен бота или ключи Supabase в публичный репозиторий:

### 1. Немедленно смените токен бота:
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **"API Token"**
5. Нажмите **"Revoke current token"** (отозвать текущий токен)
6. Создайте новый токен

### 2. Ограничьте доступ к Supabase:
1. Войдите в ваш проект на [supabase.com](https://supabase.com)
2. Перейдите в **Settings → API**
3. Проверьте настройки безопасности
4. При необходимости создайте новый проект

### 3. Удалите историю из Git (если репозиторий публичный):
```bash
# ВНИМАНИЕ: Это перепишет историю Git!
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch bot/config.py" \
  --prune-empty --tag-name-filter cat -- --all

# Или используйте BFG Repo-Cleaner (рекомендуется)
```

## ✅ Правильная настройка

### Локальная разработка:
1. Создайте файл `.env` в корне проекта (он уже в .gitignore)
2. Скопируйте `env.example` в `.env`
3. Заполните реальными значениями:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш_ключ_здесь
ADMIN_SECRET_CODE=ваш_секретный_код
```

### Деплой на Render.com:
1. В настройках сервиса (Settings → Environment Variables)
2. Добавьте все переменные из `env.example`
3. **НЕ** добавляйте их в код!

## 🔐 Что НЕ должно быть в коде:

- ❌ Токены ботов
- ❌ API ключи
- ❌ Пароли
- ❌ Секретные коды
- ❌ Приватные ключи

## ✅ Что МОЖЕТ быть в коде:

- ✅ Публичные URL (без ключей)
- ✅ Конфигурация без секретов
- ✅ Примеры значений (в .example файлах)

## 📋 Чек-лист безопасности:

- [ ] `.env` файл в `.gitignore`
- [ ] Нет захардкоженных токенов в коде
- [ ] Все секреты в переменных окружения
- [ ] `env.example` содержит только примеры
- [ ] Репозиторий проверен на наличие секретов

## 🛡️ Дополнительные меры:

1. **Используйте разные токены для dev/prod**
2. **Регулярно ротируйте ключи**
3. **Ограничьте доступ к репозиторию**
4. **Используйте секретные хранилища** (Render Environment Variables, GitHub Secrets)

